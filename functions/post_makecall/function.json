{
  "description": "go build with godep",
  "hooks": {
    "build": "dep ensure;GOOS=linux GOARCH=amd64 go build -o main *.go"
  },
  "environment": {
    "plivo_authid": "kms encrypted strings",
    "plivo_token" : "kms encrypted strings",
    "callback_url" : "todo"
  }
}